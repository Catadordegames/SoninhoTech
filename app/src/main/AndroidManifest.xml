<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SoninhoTech"
        tools:targetApi="31">
        <activity
            android:name=".cadastro_alim_activity"
            android:exported="false" />
        <activity
            android:name=".cadastro_sono_activity"
            android:exported="false" />
        <activity android:name=".esqueci_senha_activity" />
        <activity
            android:name=".login_activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".cadastro_activity" />
        <activity android:name=".tela_principal_activity" />
        <activity android:name=".relatorio_sono_activity" />
        <activity android:name=".relatorio_alimentacao_activity" />
        <activity android:name=".selecionar_perfil_activity" />
    </application>

</manifest>